import ProfileLayout from '@/app/ProfileLayout'
import React from 'react'

const page = () => {
    return (
        <ProfileLayout ProfilePages>
            groups
        </ProfileLayout>
    )
}

export default page