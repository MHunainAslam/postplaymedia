import ProfileLayout from '@/app/ProfileLayout'
import React from 'react'

const page = () => {
    return (
        <ProfileLayout ProfilePages>
            messages
        </ProfileLayout>
    )
}

export default page